<?php

// Assignment 4

// 4.1 Create a class called Validator

// 4.2 Create a public static method called validate_email, which validates if a string is an email. Returns the email if passed or false if not. Test validate_email with different strings to ensure its working properly

// 4.3 Create a public static method called sanitize_string(), which strips all tags from a string. Test sanitize_string with different strings to ensure its working properly

// 4.4 Create a public static method called valiate_int(), which validates if the parameter given is a string or a numeric string. Return as integer if pass or false if not

// 4.5 Create a public static method called validate_json(), which validates if a string is correct json. Return true if passed or false if not.



